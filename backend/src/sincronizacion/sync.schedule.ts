/* import cron from 'node-cron';
import SyncService from '../services/sync.services';

const setupSyncSchedule = () => {
  // Sincronizar cada 30 minutos
  cron.schedule('*//* 30 * * * *', async () => {
 */ //   console.log('Iniciando sincronización LDAP-MongoDB...');
   /*  try {
      await SyncService.syncExistingUsers();
      console.log('Sincronización completada');
    } catch (error) {
      console.error('Error en sincronización programada:', error);
    }
  });
 */
/*   // Sincronización inicial al arrancar
  SyncService.syncExistingUsers();
};
 */
/* export default setupSyncSchedule; */ 